# Empty catalog

A config that points at a catalog with no stories. The CLI must stay up; the workshop shows an empty nav and canvas instead of crashing.

```bash
npx schublade serve --config ./schublade.toml
# or ./run.sh
```

http://127.0.0.1:47301
